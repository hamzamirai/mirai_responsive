name: mirai_responsive
description: MiraiDevs developed the Mirai Responsive Package for Custom Sizes For Any Mobile Device.
version: 0.0.3+1
homepage: https://github.com/devhch/mirai_responsive

environment:
  sdk: ">=3.5.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
